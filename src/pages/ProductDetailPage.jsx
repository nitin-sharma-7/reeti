export const products = [
  {
    id: "p001",

    // BASIC INFO
    title: "Hand Painted Wooden Elephant",
    description:
      "Beautifully handcrafted wooden elephant figurine featuring traditional Rajasthani artwork and vibrant colors. Perfect for home décor, office desks, gifting, or ethnic interior styling.",

    // PRICING
    price: 999,
    oldPrice: 1499,
    currency: "INR",

    // MEDIA
   images: [ "/homedecor/WhatsApp Image 2026-03-10 at 5.51.36 PM.jpeg",
     "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM (1).jpeg", 
     "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM (2).jpeg",
      "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM.jpeg", "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM.jpeg", ],

    // RATING
    rating: 4.6,
    totalReviews: 124,

    // VARIANTS
   
      sizes: ["Free Size", "Small", "Medium", "Large"],
      colors: [
        { name: "Pink", value: "#f9a8d4" },
        { name: "Light Green", value: "#86efac" },
        { name: "Light Blue", value: "#7dd3fc" },
      ],
  

    // INVENTORY
    stock: 20,
    sku: "ELEPHANT-001",

    // CATEGORY / TAGS
    category: "Home Decor",
    tags: ["Home Decor", "Handicraft", "Wooden Art"],

    // EXTRA DETAILS (used in tabs)
    details: {
      material: "Wood",
      style: "Traditional Rajasthani",
      weight: "500g",
      dimensions: "10 x 5 x 8 inches",
      warranty: "No Warranty",
    },

    // FLAGS (optional but powerful)
    isFeatured: true,
    isTrending: true,
    isBestSeller: false,

    // TIMESTAMP (future use)
    createdAt: "2026-03-20",
  }, 
  {
    id: "p001",

    // BASIC INFO
    title: "Hand Painted Wooden Elephant",
    description:
      "Beautifully handcrafted wooden elephant figurine featuring traditional Rajasthani artwork and vibrant colors. Perfect for home décor, office desks, gifting, or ethnic interior styling.",

    // PRICING
    price: 999,
    oldPrice: 1499,
    currency: "INR",

    // MEDIA
   images: [ "/homedecor/WhatsApp Image 2026-03-10 at 5.51.36 PM.jpeg",
     "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM (1).jpeg", 
     "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM (2).jpeg",
      "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM.jpeg", ],

    // RATING
    rating: 4.6,
    totalReviews: 124,

    // VARIANTS
   
      sizes: ["Free Size", "Small", "Medium", "Large"],
      colors: [
        { name: "Pink", value: "#f9a8d4" },
        { name: "Light Green", value: "#86efac" },
        { name: "Light Blue", value: "#7dd3fc" },
      ],
  

    // INVENTORY
    stock: 20,
    sku: "ELEPHANT-001",

    // CATEGORY / TAGS
    category: "Home Decor",
    tags: ["Home Decor", "Handicraft", "Wooden Art"],

    // EXTRA DETAILS (used in tabs)
    details: {
      material: "Wood",
      style: "Traditional Rajasthani",
      weight: "500g",
      dimensions: "10 x 5 x 8 inches",
      warranty: "No Warranty",
    },

    // FLAGS (optional but powerful)
    isFeatured: true,
    isTrending: true,
    isBestSeller: false,

    // TIMESTAMP (future use)
    createdAt: "2026-03-20",
  },
  {
    id: "p001",

    // BASIC INFO
    title: "Hand Painted Wooden Elephant",
    description:
      "Beautifully handcrafted wooden elephant figurine featuring traditional Rajasthani artwork and vibrant colors. Perfect for home décor, office desks, gifting, or ethnic interior styling.",

    // PRICING
    price: 999,
    oldPrice: 1499,
    currency: "INR",

    // MEDIA
   images: [ "/homedecor/WhatsApp Image 2026-03-10 at 5.51.36 PM.jpeg",
     "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM (1).jpeg", 
     "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM (2).jpeg",
      "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM.jpeg", ],

    // RATING
    rating: 4.6,
    totalReviews: 124,

    // VARIANTS
   
      sizes: ["Free Size", "Small", "Medium", "Large"],
      colors: [
        { name: "Pink", value: "#f9a8d4" },
        { name: "Light Green", value: "#86efac" },
        { name: "Light Blue", value: "#7dd3fc" },
      ],
  

    // INVENTORY
    stock: 20,
    sku: "ELEPHANT-001",

    // CATEGORY / TAGS
    category: "Home Decor",
    tags: ["Home Decor", "Handicraft", "Wooden Art"],

    // EXTRA DETAILS (used in tabs)
    details: {
      material: "Wood",
      style: "Traditional Rajasthani",
      weight: "500g",
      dimensions: "10 x 5 x 8 inches",
      warranty: "No Warranty",
    },

    // FLAGS (optional but powerful)
    isFeatured: true,
    isTrending: true,
    isBestSeller: false,

    // TIMESTAMP (future use)
    createdAt: "2026-03-20",
  }, 
  {
    id: "p001",

    // BASIC INFO
    title: "Hand Painted Wooden Elephant",
    description:
      "Beautifully handcrafted wooden elephant figurine featuring traditional Rajasthani artwork and vibrant colors. Perfect for home décor, office desks, gifting, or ethnic interior styling.",

    // PRICING
    price: 999,
    oldPrice: 1499,
    currency: "INR",

    // MEDIA
   images: [ "/homedecor/WhatsApp Image 2026-03-10 at 5.51.36 PM.jpeg",
     "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM (1).jpeg", 
     "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM (2).jpeg",
      "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM.jpeg", "/homedecor/WhatsApp Image 2026-03-10 at 5.51.37 PM.jpeg", ],

    // RATING
    rating: 4.6,
    totalReviews: 124,

    // VARIANTS
   
      sizes: ["Free Size", "Small", "Medium", "Large"],
      colors: [
        { name: "Pink", value: "#f9a8d4" },
        { name: "Light Green", value: "#86efac" },
        { name: "Light Blue", value: "#7dd3fc" },
      ],
  

    // INVENTORY
    stock: 20,
    sku: "ELEPHANT-001",

    // CATEGORY / TAGS
    category: "Home Decor",
    tags: ["Home Decor", "Handicraft", "Wooden Art"],

    // EXTRA DETAILS (used in tabs)
    details: {
      material: "Wood",
      style: "Traditional Rajasthani",
      weight: "500g",
      dimensions: "10 x 5 x 8 inches",
      warranty: "No Warranty",
    },

    // FLAGS (optional but powerful)
    isFeatured: true,
    isTrending: true,
    isBestSeller: false,

    // TIMESTAMP (future use)
    createdAt: "2026-03-20",
  },
];
import { useState } from "react";
import CommonBanner from "../components/common/CommonBanner";
import ShopColumnDescription from "../components/shop/ShopColumnDescription";
import ContactSection from "../components/common/ContactSection";
import { FaStar } from "react-icons/fa";
import { CiShoppingCart } from "react-icons/ci";

const ProductGallery = ({ images }) => {
  const [active, setActive] = useState(0);

  return (
    <div className="overflow-hidden ">
      <img
        src={images[active]}
        className="w-100 h-auto object-contain rounded-2xl overflow-hidden "
      />

      <div className="flex gap-3 mt-3">
        {images.map((img, i) => (
          <img
            key={i}
            src={img}
            onClick={() => setActive(i)}
            className={`w-20 h-20 object-cover rounded cursor-pointer border ${
              active === i ? "border-black" : ""
            }`}
          />
        ))}
      </div>
    </div>
  );
};




const ProductInfo = ({ product }) => {
  const [qty, setQty] = useState(1);
  const [selectedSize, setSelectedSize] = useState(0);
  const [selectedColor, setSelectedColor] = useState(0);

  return (
    <div className="space-y-5">

      {/* TITLE */}
      <h1 className="text-3xl font-semibold text-gray-800">
        {product.title}
      </h1>

      {/* DESCRIPTION */}
      <p className="text-gray-500 text-sm leading-relaxed">
        {product.description}
      </p>

      {/* PRICE + RATING */}
      <div className="flex justify-between items-center">
        <h2 className="text-2xl font-bold text-gray-900">
          ₹{product.price}
        </h2>

        <div className="flex items-center gap-2 text-sm">
          <div className="text-yellow-400 flex">{[1,2,3,4,5].map((star, i) => (
            <FaStar key={i} />
          ))}</div>
          <span className="text-gray-500">
            {product.rating} Rating
          </span>
        </div>
      </div>

      {/* TAGS */}
      <div className="text-sm text-gray-500">
        <span className="font-medium">Tags :- </span>
        <span className="text-[var(--color-brand)]">
          {product.tags.join(", ")}
        </span>
      </div>

      {/* SIZE */}
      <div>
        <h4 className="text-sm font-medium mb-2">Product Size</h4>
        <div className="flex gap-2">
          {product.sizes?.map((size, i) => (
            <button
              key={i}
              onClick={() => setSelectedSize(i)}
              className={`px-3 py-1 text-sm rounded-md border ${
                selectedSize === i
                  ? "bg-[var(--color-brand)] text-white border-[var(--color-brand)]"
                  : "bg-white text-gray-600 border-gray-300"
              }`}
            >
              {size}
            </button>
          ))}
        </div>
      </div>

      {/* QUANTITY */}
      <div>
        <h4 className="text-sm font-medium mb-2">Select quantity</h4>
        <div className="flex items-center w-fit border rounded-md overflow-hidden">
          <button
            onClick={() => setQty(qty > 1 ? qty - 1 : 1)}
            className="px-3 py-1 bg-gray-100 hover:bg-gray-200"
          >
            −
          </button>
          <span className="px-4">{qty}</span>
          <button
            onClick={() => setQty(qty + 1)}
            className="px-3 py-1 bg-gray-100 hover:bg-gray-200"
          >
            +
          </button>
        </div>
      </div>

      {/* COLORS */}
      <div>
        <h4 className="text-sm font-medium mb-2">Select the color</h4>
        <div className="flex gap-2">
          {product.colors?.map((color, i) => (
            <div
              key={i}
              onClick={() => setSelectedColor(i)}
              className={`w-6 h-6 rounded-full cursor-pointer border-2 ${
                selectedColor === i
                  ? "border-black scale-110"
                  : "border-gray-200"
              }`}
              style={{ backgroundColor: color }}
            />
          ))}
        </div>
      </div>

      {/* BUTTONS */}
      <div className="flex gap-3 pt-4">

        {/* ADD TO CART */}
        <button className="bg-[var(--color-brand)] text-white px-4 py-2 rounded-md text-sm hover:opacity-90 transition flex items-center gap-2">
          <CiShoppingCart size={20} />Add To Cart
        </button>

        {/* BUY NOW */}
        <button className="bg-black text-white px-4 py-2 rounded-md text-sm hover:opacity-90 transition flex items-center gap-2">
         Buy Now
        </button>

      </div>
    </div>
  );
};


const ProductTabs = ({ product }) => {
  const [tab, setTab] = useState("desc");

  const tabs = [
    { id: "desc", label: "Description" },
    { id: "info", label: "Additional Information" },
    { id: "review", label: "Product Review" },
  ];

  return (
    <div className="max-w-7xl mx-auto mt-12 px-4">

      {/* PREMIUM TABS */}
      <div className="flex gap-3 mb-8">
        {tabs.map((item) => (
          <button
            key={item.id}
            onClick={() => setTab(item.id)}
            className={`px-5 py-2 rounded-full text-sm transition ${
              tab === item.id
                ? "bg-[var(--color-brand)] text-white shadow-sm"
                : "bg-gray-100 text-gray-600 hover:bg-gray-200"
            }`}
          >
            {item.label}
          </button>
        ))}
      </div>

      {/* CONTENT */}
      <div>

        {/* DESCRIPTION */}
        {tab === "desc" && (
          <div className="text-gray-600 space-y-4 text-sm leading-relaxed max-w-3xl">
            <p>{product.description}</p>

            <ul className="space-y-2">
              <li className="flex gap-2">
                <span className="text-[var(--color-brand)]">✔</span>
                Premium handcrafted quality
              </li>
              <li className="flex gap-2">
                <span className="text-[var(--color-brand)]">✔</span>
                Perfect for home décor & gifting
              </li>
            </ul>
          </div>
        )}

        {/* ADDITIONAL INFO */}
        {tab === "info" && (
          <div className="bg-gray-50 rounded-xl p-4 max-w-3xl">
            {[
              { label: "Size", value: "Free Size" },
              { label: "Color", value: product.colors.map((color) => color.name).join(", ") },
              { label: "Rating", value: `${product.rating}` },
              { label: "Material", value: "Wood" },
              { label: "Stock", value: product.stock },
            ].map((item, i) => (
              <div
                key={i}
                className="flex justify-between py-3 text-sm border-b last:border-none border-gray-200"
              >
                <span className="text-gray-500">{item.label}</span>
                <span className="font-medium text-gray-800">
                  {item.value}
                </span>
              </div>
            ))}
          </div>
        )}

        {/* REVIEWS */}
        {tab === "review" && (
          <div className="space-y-6 max-w-3xl">

            {/* Review Cards */}
            {[1, 2, 3].map((_, i) => (
              <div
                key={i}
                className="bg-gray-50 p-5 rounded-xl shadow-sm"
              >
                <div className="flex gap-4">
                  <div className="w-14 h-14 bg-gray-200 rounded-lg flex items-center justify-center text-xs">
                    IMG
                  </div>

                  <div className="flex-1">
                    <div className="flex justify-between items-center">
                      <h4 className="font-medium text-sm">
                        Cobus Bester
                      </h4>
                      <div className="text-yellow-500 text-sm">
                        <div className="text-yellow-400 flex">{[1,2,3,4,5].map((star, i) => (
                          <FaStar key={i} />
                        ))}</div>
                      </div>
                    </div>

                    <p className="text-xs text-gray-400 mt-1">
                      March 7, 2013
                    </p>

                    <p className="text-sm text-gray-600 mt-2 leading-relaxed">
                      Lorem Ipsum is simply dummy text of the printing and
                      typesetting industry.
                    </p>
                  </div>
                </div>
              </div>
            ))}

            {/* ADD REVIEW */}
            <div className="bg-gray-50 p-6 rounded-xl shadow-sm">
              <h3 className="text-sm font-semibold mb-4">
                ADD A REVIEW
              </h3>

              <div className="space-y-4">

                <div>
                  <label className="text-xs text-gray-500">Name *</label>
                  <input className="w-full mt-1 p-2 rounded-lg bg-white border border-gray-200 focus:outline-none focus:ring-1 focus:ring-[var(--color-brand)]" />
                </div>

                <div>
                  <label className="text-xs text-gray-500">Email *</label>
                  <input className="w-full mt-1 p-2 rounded-lg bg-white border border-gray-200 focus:outline-none focus:ring-1 focus:ring-[var(--color-brand)]" />
                </div>

                <div>
                  <label className="text-xs text-gray-500">
                    Your Rating
                  </label>
                  <div className="flex gap-1 mt-1 text-gray-300 cursor-pointer text-lg">
                    {Array(5)
                      .fill("")
                      .map((_, i) => (
                        <span key={i} className="hover:text-yellow-400">
                          ★
                        </span>
                      ))}
                  </div>
                </div>

                <div>
                  <label className="text-xs text-gray-500">
                    Your Review
                  </label>
                  <textarea className="w-full mt-1 p-2 h-28 rounded-lg bg-white border border-gray-200 focus:outline-none focus:ring-1 focus:ring-[var(--color-brand)]" />
                </div>

                <button className="bg-[var(--color-brand)] text-white px-6 py-2 rounded-lg text-sm hover:opacity-90 transition">
                  Submit
                </button>
              </div>
            </div>

          </div>
        )}
      </div>
    </div>
  );
};


const RelatedProducts = ({ products }) => {
  return (
    <div className="max-w-7xl mx-auto mt-10 p-4">
      <h2 className="text-xl font-bold mb-4">Related Products</h2>

      <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
        {products.map((item) => (
          <div key={item.id} className="border p-2 rounded-lg">
            <img
              src={item.images[0]}
              className="w-full h-auto object-contain rounded"
            />
            <h3 className="text-sm mt-2">{item.title}</h3>
            <p className="font-semibold">₹{item.price}</p>
          </div>
        ))}
      </div>
    </div>
  );
};


export const ProductDetailPage = () => {
    const  product = products[products?.length-1];
    return (
       <div className="">
        <CommonBanner title="Product Detail"/>
      <div className="max-w-7xl mx-auto px-4 py-10 grid md:grid-cols-2 gap-4">
        <ProductGallery images={product.images} />
        <ProductInfo product={product} />
      </div>

      <ProductTabs product={product} />
      <RelatedProducts products={products} />
      <div className="max-w-7xl mx-auto px-4">
        <ShopColumnDescription />
      </div>
      <ContactSection />
    </div>
    )
}